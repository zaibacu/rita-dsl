[tox]
envlist = py36, py37, py38, codecoverage-report

[testenv]
deps =
    pytest
    pytest-benchmark
    pytest-cov
commands =
    python -m pytest --cov=rita --cov-report xml tests/

[testenv:codecoverage-report]
basepython = python3.8
deps = codecov
commands = codecov --token=$CODECOV_TOKEN
