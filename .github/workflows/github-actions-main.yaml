name: Tox
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.9' ]
    name: Python ${{ matrix.python-version }} sample
    steps:
      - uses: actions/checkout@v2
      - name: Setup python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
          architecture: x64
      - run: pip install tox
      - run: CODECOV_TOKEN=${{ secrets.CODECOV_TOKEN }} tox -e py39
  build:
    name: spaCy 3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.9'
          architecture: 'x64'
      - run: pip install tox
      - run: CODECOV_TOKEN=${{ secrets.CODECOV_TOKEN }} tox -e spacy3

